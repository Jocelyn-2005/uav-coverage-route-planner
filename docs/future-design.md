# 后续设计边界

- 更高级的局部搜索可作为资源受限条件下的降本增效研究，但当前不实施；
- 覆盖生成、lane 选择和排序的一体化联合优化只作理论方向，不等同于必须采用端到端模型；
- 任务时间、重复覆盖、转弯和能耗可形成多目标函数，但应先证明优化收益高于计算和调参成本；
- 地形跟随、斜视相机、动态搜索、轨迹平滑和多机协调不属于本分支职责；
- 后续若恢复墙体遮挡可见性，必须单独评估其在目标硬件上的时间和内存成本。
